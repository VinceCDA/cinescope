{% extends 'base.html.twig' %}

{% block title %}Plateforme index{% endblock %}

{% block body %}
    <h1>Plateforme index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Url</th>
                <th>Logo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for plateforme in plateformes %}
            <tr>
                <td>{{ plateforme.id }}</td>
                <td>{{ plateforme.name }}</td>
                <td>{{ plateforme.url }}</td>
                <td>{{ plateforme.logo }}</td>
                <td>
                    <a href="{{ path('app_plateforme_show', {'id': plateforme.id}) }}">show</a>
                    <a href="{{ path('app_plateforme_edit', {'id': plateforme.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
